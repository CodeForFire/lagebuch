<Project Sdk="Microsoft.NET.Sdk">

  <!-- The embedded host: Kestrel + SignalR exposing one open incident over Tailscale. ASP.NET Core
       only targets net8.0 (never net8.0-android), so this project is referenced by the desktop head
       ONLY — never by AppLogic or the Android head. -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\LageBuch.AppLogic\LageBuch.AppLogic.csproj" />
    <ProjectReference Include="..\LageBuch.Sync\LageBuch.Sync.csproj" />
  </ItemGroup>

</Project>
