# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
.vs/
TestResults/
coverage/
*.trx
artifacts/

# Project-specific: never commit the legacy template or planning artifacts
*.xltm
Einsatzdokumentation_Digital_*.xltm
docs/superpowers/

# Real master data (streets, stations, call signs) and the personnel roster are PII and are
# never compiled in or committed — they only enter a running install through the editor's Import.
# docs/master-data.example.json documents the format and is safe to track.
seed-source/
*.masterdata.json

# Local SDD scratch / ledger
.superpowers/

# Local Claude Code state
.claude/

# Generated voice/audio drafts (filenames can carry voice-artist names)
*.mp3
